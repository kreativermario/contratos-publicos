[project]
name = "contratos-ingest"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["contratos-core"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["contratos_ingest*"]
