# One command for all three packages. `testpaths` is what keeps a bare `pytest`
# from walking web/node_modules looking for python it will never find.
[pytest]
testpaths = core/tests api/tests ingest/tests
addopts = -q
